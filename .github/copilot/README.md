# Copilot Notes

Repository-level Copilot settings do not enforce model selection. Keep model choice in user/session settings.

Suggested user-level default:

```jsonc
// ~/.copilot/settings.json
{
  "model": "auto",
  "effortLevel": "low"
}
```

Use `/model` only when a task needs escalation.
